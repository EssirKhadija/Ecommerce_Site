import React from "react";

export default function Footer() {
  return (
    <footer className="text-center p-3 mt-4">
        © {new Date().getFullYear()} MonEshop — Tous droits réservés
    </footer>

  );
}
